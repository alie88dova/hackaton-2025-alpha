server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # ВСЕ запросы отдаём через index.html — стандарт для SPA
    location / {
        try_files $uri $uri/ /index.html;
    }
}